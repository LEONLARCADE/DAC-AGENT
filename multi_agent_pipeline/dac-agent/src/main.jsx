import React from 'react'
import ReactDOM from 'react-dom/client'
import DAC_AGENT from './DAC_AGENT'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DAC_AGENT />
  </React.StrictMode>
)